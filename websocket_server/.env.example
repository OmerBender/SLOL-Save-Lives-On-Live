# Optional shell environment values read by websocket_server/server_websocket.py
# The server does not require python-dotenv; export these values in the shell or service environment.

RESCUE360_DEMO_VIDEO_DIR=demo_videos
RESCUE360_DASHBOARD_SLOTS=5
RESCUE360_DASHBOARD_FRAME_INTERVAL_SEC=0.4
RESCUE360_DASHBOARD_HISTORY_LIMIT=500
RESCUE360_DEMO_TARGET_FPS=8
